import { TreeStateDirective } from './tree-state.directive';

describe('TreeStateDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeStateDirective();
    expect(directive).toBeTruthy();
  });
});
